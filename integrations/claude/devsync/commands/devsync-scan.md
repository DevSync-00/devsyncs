---
description: Scan the current workspace for schema drift
argument-hint: "[local]"
---

Use the DevSync tools to scan the current workspace. If `$ARGUMENTS` contains `local`, run an offline local scan. Summarize mismatches by severity and do not apply changes.
